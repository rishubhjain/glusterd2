package device

// This file contains helper functions facilitate easier interaction with the
// device information stores in the store

import (
)

const (
        devicePrefix string = "devices/"
)


//func GetDevice(id string) (*Peer,error) {
